{'v': 1, 'model_id': 'sha3:abcd...', 'input_hash': 'sha3:1234...', 'output_hash': 'sha3:5678...', 'provider_id': 'spiritclawd-node-7', 'timestamp': '2025-04-25T22:30:00Z', 'metrics': {'tokens': 142, 'input_tokens': 45, 'output_tokens': 97, 'latency_ms': 215}, 'attestation': {'type': 'nitro', 'report': '<base64-encoded-attestation-document>', 'ca_cert': '<base64-encoded-nitro-ca-chain>'}, 'sig': 'ed25519:abcdef...', 'Canonicalization': 'Receipt bytes are CBOR canonical form (RFC 8949). Signature covers all fields except `sig`.\n\n**Model ID derivation:**  \n- Provider pins a specific model file (GGUF). Computes SHA3-256 of the entire file bytes.  \n- Optionally include architecture string (e.g.', 'llama:7b:q4_0': 'to avoid collisions.  \n- Model ID is registered in Patronaige’s model registry (so builders can verify they’re using the expected weights).\n\n---\n\n## 4. Provider Flow\n\n1. **Initialization**  \n   - Provider spins up TEE instance (AWS Nitro Enclave', 'enclave': 'a. Compute `input_hash = sha3(prompt + params)`  \n     b. Compute `output_hash = sha3(completion + metrics)`  \n     c. Build receipt CBOR (without `sig`).  \n     d. Sign receipt with enclave Ed25519 private key.  \n     e. Attach signature and Nitro attestation report.  \n   - Provider returns `{completion', 'receipt}`': 1.0, 'proof)': 'a. Check Nitro/SGX report signature against known CA roots.  \n   b. Verify report’s `measurement` (PCR hash) matches expected enclave code hash (registered).  \n   c. Verify report is not expired and was generated within last N hours.  \n4. **Check model_id** matches expected model (query Registry if needed).  \n5. **Check input_hash** matches the prompt they sent (recompute).  \n6. **Check output_hash** matches the received `completion + metrics`.  \n7. If all checks pass → accept inference as genuine.  \n8. Store receipt; optionally verify anchoring exists (receipt_digest in Registry batch).\n\nIf any check fails → initiate dispute within dispute window (e.g.', 'vote': 'VALID` (output matches builder’s completion) or `INVALID` (mismatch). Votes are signed.\n5. **Outcome** — If majority `VALID`', 'stake': 'Optional; to prevent frivolous disputes', 'reward': 'Small fee from dispute loser for re-execution work.\n\n---\n\n## 7. Registry & Anchoring\n\n### Registry Contract (L2)\n\n**Storage:**  \n- `providers[provider_id] => {pubkey', 'Functions': 'register_provider(pubkey', 'batcher': 'collects receipt digests for ~1 hour', 'amortization': 0.001, 'sample': 'load a tiny Llama 3.2 1B (quantized) and run forward pass.  \n- Generate Ed25519 key inside enclave', 'Arbitrum)': 'provider registration', 'MVP': 'only `register` and `anchor`; disputes off-chain for now.\n\n### Week 7-8: End-to-End Test\n\n- Deploy TEE worker on real AWS Nitro instance.  \n- Run inference', 'simulation': 'committee re-runs', 'to': 1.0, 'metric': 'Demonstrate a full LLM ZK proof < 5 minutes (even if expensive). Then decide when to flip.\n\n---\n\n## 11. Open Questions\n\n- **Model weight distribution:** Who hosts the model files? Providers download from Registry? CDN?  \n- **Enclave image signing:** How do we ensure all providers run identical enclave code? Use Docker/OCI image hash in measurement.  \n- **Privacy:** Receipt reveals input/output hashes', 'Jurisdiction': 'TEE attestation may not be legally admissible everywhere. Need legal review if we’re selling audit compliance.  \n- **Multi-provider inference:** For distributed inference (tensor parallelism) receipts need aggregation. Out of scope for v1.\n\n---\n\n## 12. Risks & Mitigations\n\n| Risk | Likelihood | Impact | Mitigation |\n|------|------------|--------|------------|\n| TEE compromise (Nitro vuln) | Low | Critical | Have ZK fallback plan; monitor hardware CVEs |\n| Committee bribery | Medium | High | Random selection', 'audit logs': 'ot evidence |\n| High gas costs on L2 for anchoring | Medium | Low | Batch aggressively; use Polygon or zkSync if cheaper |\n\n---\n\n## 13. Conclusion\n\nThis spec gives Patronaige an **actionable audit layer** using today’s TEE tech', 'step': 'Implement Week 1-2 prototype** and validate technical feasibility with a real Nitro enclave.\n\n---\n\n*“If you can’t measure it'}